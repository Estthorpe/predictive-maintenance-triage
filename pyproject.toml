[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I"]   # errors, pyflakes, import sorting

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]   # <-- include the project root so "import src..." works
